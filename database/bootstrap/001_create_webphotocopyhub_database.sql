-- Bootstrap PostgreSQL database for WebPhotocopyHub.
-- Run this script from a maintenance database such as postgres.
-- PostgreSQL folds unquoted database names to lower-case, so keep the quotes.

CREATE DATABASE "WebPhotocopyHub";
